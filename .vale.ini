StylesPath = styles
MinAlertLevel = error

# BasedOnStyles MUST live under a glob section, not at the top level —
# at global scope vale ignores it and lints nothing.
[*.{md,txt}]
BasedOnStyles = Editorial
